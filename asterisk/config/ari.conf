; ===========================================
; Asterisk REST Interface (ARI) Configuration
; AI Voice Calling Platform
; ===========================================

[general]
enabled=yes
pretty=yes
allowed_origins=*

[aibrain]
type=user
read_only=no
password=CHANGE_THIS_PASSWORD
